output {
  elasticsearch {
    host => "{{ ansible_ssh_host }}:9200"
    cluster => "{{ es_cluster }}"
    index => "logstash-%{+YYYY.MM.dd}"
    protocol => "http"
    user => "{{ logstash_user }}"
    password => "{{ logstash_password }}"
  }
  stdout { codec => rubydebug }
}
